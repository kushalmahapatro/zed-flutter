id = "flutter"
name = "Flutter"
description = "Flutter run/debug integration. Install the Dart extension for the Dart/Flutter language server and debug adapter (breakpoints, isolates, threads). This extension adds debug locators that turn Flutter Zed tasks into Dart-adapter debug sessions, plus starter launch presets."
version = "0.2.0"
schema_version = 1
authors = ["Kushal"]
repository = "https://github.com/kushalmahapatro/zed-flutter"

[debug_locators.flutter]
